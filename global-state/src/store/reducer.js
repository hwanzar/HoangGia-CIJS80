const initState = {
    todos: [],
    todoInput: "",
};

function reducer(state, action) {
    switch (
        action.type
        //action
    ) {
    }
}

export { initState };
export default reducer;
