const Calculator = () => {
    const a = 10;
    const b = 15;
    return (
        <div>
            <p>
                The result of {a} + {b} is {a + b}.
            </p>
        </div>
    );
};

export default Calculator;
